# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Health Journal' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Health Journal
	pod 'Firebase/Core'
	pod 'Firebase/Database'
	pod 'Firebase/Firestore'
	pod 'Firebase/Storage'
	pod 'Firebase/Messaging'
	pod 'Firebase/Auth'
	pod 'JGProgressHUD'
	pod 'Alamofire'

  target 'Health JournalTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Health JournalUITests' do
    # Pods for testing
  end

end
